
<%= link_to_if(@users_count > 0, "Users (#{@users_count})", 
search_users_path(params[:search]) ) do "Users (0)" end %>

<% if @tweets_count > 0 %>
	<%= link_to "Tweets (#{@tweets_count})", search_tweets_path(:search => params[:search]) %>
<% else %>
	No tweets
<% end %>
<br />

<% @users_searched.each do |user| %>
	<%= link_to user.name, user %>
<% end %>
